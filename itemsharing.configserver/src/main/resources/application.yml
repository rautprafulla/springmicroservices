server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rautprafulla/config-repo
          username: rautprafulla
          password: praf007
          search-paths: userservice, itemservice, authorizationservice, zuulservice, zipkinservice
        encrypt:
          enabled: false